version: 1.0.0
events:
  build:
    steps:
      - hello:
          type: script
          script:
            - patch --version
            - git --version
            - mkdir docroot
            - echo "Dane is the best!" > docroot/index.html
